# Sidsru Blog

Personal blog built with Jekyll and the Chirpy theme.

## Local Development

```console
bundle install
npm install
npm run build
bundle exec jekyll serve
```

Posts live in `_posts` and should use the `YYYY-MM-DD-title.md` filename format.
